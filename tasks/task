
在邮件的 view 界面，添加一个 reply button。
点击 replay button，在 view 界面上分添加一个回复框。具体参考outlook 的回复框。

我要在这个基础上，view 上方本来是一个回复框，现在我要做成一半回复框，一半是 chat。

先不用实现 chat，留白就好。


不需要 refresh 和 sync 两个按钮。把header 变成跟紧凑。

mail 加载和同步设计成，每两分钟自动从服务器同步。
进入页面的时候，先加载邮件列表，也就是本来的 sync。
然后增量同步更新，每两分钟同步一次。








